const express = require('express')
const app = express()
const port = 3000

app.get('/greet', (req, res) => {
  res.send('Hello it me Hajira Imran!')
})

app.listen(port, () => {
  console.log(`Example app listening on port ${port}`)
})